# 플레이말로우 모바일 네이티브 전환 검토

> 검토일: 2026-08-05
> 대상: Android-first 네이티브 앱
> 결론: **Godot 4.7.1 stable을 권장하고, 현재 웹/PWA는 별도 서비스로 유지한다.**

## 1. 한 줄 결론

플레이말로우는 단순한 단일 게임이 아니라 30개 이상의 미니게임, 오늘의 미션, 기록, 두뇌 영역, 다국어, 공유 흐름을 가진 **게임형 서비스**다. 따라서 2D 렌더링만 빠른 엔진보다 UI·저장·Android 연동·오디오를 함께 관리하기 쉬운 Godot가 적합하다.

전체를 한 번에 포팅하지 않고 다음 순서로 간다.

```text
현재 웹/PWA 유지
      ↓
Godot Android vertical slice
      ↓
공통 앱 셸·저장·사운드·디자인 시스템 확정
      ↓
게임을 묶음 단위로 추가 포팅
      ↓
Google Play 내부 테스트 → 공개 서비스
```

## 2. 현재 저장소 진단

### 확인된 자산

| 항목 | 현재 상태 | 모바일 전환 의미 |
|---|---|---|
| 앱 셸 | `index.html` 약 9,800줄, `brain_app.html` 약 9,500줄 | 화면과 게임 로직을 분리해야 유지보수가 가능 |
| 게임 화면 | 공통 앱 안에 30개 이상 | 콘텐츠 자체는 풍부하므로 초기에는 3개만 선별 |
| 게임 데이터 | `HOME_GAMES`, `ABILITY_MAP`, `GAME_REF`, 미션 풀 | Godot용 정적 데이터/리소스 파일로 옮길 수 있음 |
| 기록 | `localStorage`의 `brain.*` 키와 백업 코드 | 네이티브 `user://` 저장소 + `MLW1` 가져오기로 연결 |
| 사운드 | WebAudio 합성음, `sfx()` 공통 이벤트, 진동 | 이벤트 설계는 재사용하고 실제 음원·믹서 품질은 새로 고도화 |
| 공유/분석 | Web Share, GA4, challenge URL | Android Share Sheet·분석 SDK·딥링크로 별도 연결 |
| PWA | manifest, service worker, 홈 화면 설치 흐름 | 웹 서비스 유지에 유효하지만 네이티브 앱의 기반으로 재사용하지 않음 |
| 이미지 | 게임 배경 35종, 총 약 1.25MB | 재사용 가능하나 모바일용 해상도·압축·아틀라스 재정리 필요 |

### 강점

- 게임 수와 인지 영역 구성이 이미 충분하다.
- 일일 미션, 기록, 개인 능력 영역, 추천 흐름이 있어 앱 리텐션 구조가 있다.
- Mallow 마스코트, 축별 색, 공통 손맛 FX, 진동·효과음 호출 지점이 이미 있다.
- 웹 버전에서 콘텐츠를 계속 검증할 수 있으므로 네이티브 앱을 실험장으로만 만들 필요가 없다.

### 핵심 리스크

- HTML 하나에 DOM·CSS·게임 상태·저장·분석이 강하게 결합되어 있어 자동 변환 포팅은 현실적이지 않다.
- `localStorage` 기록은 Android 앱과 직접 공유되지 않는다. 기존 기록은 백업 코드 가져오기를 공식 마이그레이션 경로로 삼아야 한다.
- 웹용 URL·Web Share·GA4·광고·서비스 워커는 Android에서 같은 방식으로 동작하지 않는다.
- 현재 워크스페이스에는 Godot, LÖVE, Android SDK, ADB, JDK가 설치되어 있지 않다. 따라서 이 검토에서 APK 빌드 성공을 주장할 수 없다.

## 3. 엔진 선택

| 기준 | Godot 4.7.1 | LÖVE 11.5 | 판단 |
|---|---|---|---|
| 2D 게임 | 충분함 | 매우 강함 | 둘 다 가능 |
| 앱형 UI | `Control`, `Container`, Theme, Scene 구조 | 직접 레이아웃·상태·포커스 구현 | Godot 우세 |
| 모바일 입력 | 입력 맵, 포인터/터치 이벤트, 화면 대응 | `love.touch` 기반 직접 처리 | Godot 우세 |
| 사운드 | AudioStreamPlayer, 버스, 믹서, 리소스 관리 | `love.audio`는 좋지만 믹싱·콘텐츠 파이프라인을 직접 구성 | Godot 우세 |
| Android 서비스 연동 | Android library·Gradle·Java/Kotlin 플러그인 | `love-android` 소스/임베드·Gradle 관리 비중 큼 | Godot 우세 |
| 기록·설정·화면 전환 | 씬/Autoload/리소스 설계에 자연스러움 | Lua 모듈을 직접 조립 | Godot 우세 |
| 웹 병행 | 가능하지만 웹 버전은 현재 것을 유지하는 편이 안전 | 가능하지만 동일하게 별도 관리 필요 | 동률 |
| 팀 학습 비용 | GDScript + 에디터 학습 필요 | Lua는 가볍지만 앱 구조를 더 많이 직접 설계 | 서비스 규모에서는 Godot 우세 |

### 권장 버전과 언어

- 엔진: **Godot 4.7.1 stable**로 고정하고, 초기 마이너 업그레이드는 수동 검증 후 적용한다.
- 언어: **GDScript**. Android C# 경로는 초기 선택에서 제외한다.
- 렌더링: 2D 중심. 미니게임의 필요 지점만 `Node2D`/`CanvasItem`으로 만들고, 앱 셸은 `Control`/`Container` 중심으로 구성한다.
- 화면: 세로 고정부터 시작하되 작은 Android 화면, 큰 폰, 태블릿 폭을 모두 허용하는 앵커/컨테이너 레이아웃으로 만든다.

LÖVE는 다음 조건이면 대안이 될 수 있다.

- 서비스 셸보다 순수 2D 게임 플레이가 제품의 대부분일 때
- Lua 중심 팀이고 Android 플러그인·결제·분석·알림을 직접 유지할 수 있을 때
- 앱형 UI보다 빠른 게임 프로토타이핑과 작은 바이너리가 우선일 때

현재 플레이말로우의 범위에는 이 조건보다 Godot 쪽의 유지보수 이점이 크다.

## 4. Android-first 제품 범위

### Vertical slice 3종

첫 번째 앱 빌드는 게임 수가 아니라 **공통 경험을 검증하는 것**이 목표다.

1. `flash` — 단순한 기억 게임: 카드/타이밍/결과 화면
2. `bubble` — 계산 게임: 터치 판정, 연속 콤보, 숫자 피드백
3. `trace` — 협응 게임: 드래그, 포인터 캡처, 오차·진동·사운드

이 조합은 카드 UI, 빠른 탭, 연속 드래그, 기록 저장, 효과음, 햅틱, 결과 추천을 한 번에 검증한다. `run`, `sort`, `nono`처럼 포팅 비용이나 입력 예외가 큰 게임은 공통 기반이 안정된 뒤 추가한다.

### 첫 앱에 포함

- 홈: 오늘의 3게임, 이어하기, 최근 기록
- 게임 목록: 인지 영역별 탐색
- 게임 실행: 일시정지, 종료 확인, 사운드/햅틱 상태 반영
- 결과: 점수·최고 기록·다음 게임·공유·추천
- 기록: 게임별 PB, 플레이 횟수, 일일 미션
- 설정: 언어, 사운드, 햅틱, 음악, 알림은 초기에는 토글만 제공
- 오프라인 플레이와 로컬 저장
- Android 시스템 뒤로가기, 앱 중단/복귀, 오디오 포커스 처리

### 첫 앱에서 제외

- 로그인/서버 동기화
- 실시간 랭킹
- 광고 SDK와 결제
- 30개 전 게임 동시 포팅
- 기존 웹 기록의 자동 동기화

이 기능들은 내부 테스트에서 반복 플레이와 오류율을 확인한 후 추가한다.

## 5. 모바일 UX 설계 원칙

### 앱 셸

- 기본 방향은 세로 모드, 한 손 조작 기준이다.
- 하단 탭은 `홈 / 게임 / 내 기록` 3개로 유지한다.
- 상단에는 로고를 과하게 키우지 않고 현재 화면명, 뒤로가기, 상태만 둔다.
- 안전 영역을 포함해 노치·제스처 바와 겹치지 않게 한다.
- 주요 터치 대상은 최소 48dp, 게임 타일은 오작동을 줄이기 위해 56dp 이상을 기본으로 한다.
- 화면 전환은 짧은 이동/페이드, 게임 중에는 모션을 줄이고 피드백을 선명하게 한다.

### 게임 화면

```text
상단: 뒤로가기 | 게임명 | 남은 시간/일시정지
중앙: 게임 플레이 영역(화면의 주 집중 영역)
하단: 입력/보조 조작 + 짧은 상태 문구
종료: 결과 카드 → 다음 게임(주 버튼) → 공유/추천(보조)
```

- 한 화면에 동등한 CTA를 2개 이상 두지 않는다.
- 오답은 색만으로 알리지 않고 짧은 움직임·소리·진동·문구를 조합한다.
- 시간 제한 게임은 상단 고정 타이머와 마지막 5초 신호를 사용한다.
- 결과 화면은 숫자 하나를 가장 크게 보여 주고, 최고 기록이면 축하 연출을 강화한다.

### 디자인 고도화

- 이모지 중심 아이콘을 줄이고 Mallow·게임 오브젝트를 동일한 벡터/일러스트 스타일로 재제작한다.
- 공통 토큰: `mallow`, `ink`, `surface`, `accent`, `success`, `warning`, `danger`, `radius`, `space`, `motion`.
- 게임별 색은 인지 축에 연결하되, 색맹 사용자를 위해 아이콘·문구·패턴을 함께 사용한다.
- 카드·버튼은 평면 그라데이션보다 명확한 깊이, 눌림, 선택, 비활성 상태를 설계한다.
- Mallow 표정은 `idle / ready / good / miss / win` 5종을 공통 컴포넌트로 만든다.
- 초기 다크 모드는 반드시 동시에 만들지 않고, 색 토큰을 분리해 후속 작업으로 열어 둔다.

## 6. 사운드·햅틱 고도화

현재 WebAudio의 `good/bad/win/tap/flip/whoosh/tick` 호출 구조는 이벤트 목록의 초안으로 활용할 수 있지만, 합성음만 그대로 옮기지는 않는다.

### 사운드 계층

1. `Music`: 홈/게임 분위기 루프, 기본 꺼짐 또는 매우 낮은 볼륨
2. `UI`: 탭, 선택, 뒤로가기, 토글
3. `Gameplay`: 정답, 오답, 콤보, 시간 경고
4. `Result`: 완료, 최고 기록, 미션 완료
5. `Voice/Accessibility`: 초기에는 보류, 필요한 문구부터 선택적으로 추가

### 품질 기준

- 같은 이벤트는 모든 게임에서 같은 음색을 공유한다.
- 오답은 큰 소리보다 짧고 낮은 소리로 처리해 반복 피로를 줄인다.
- 콤보는 음정 상승, 최고 기록은 짧은 3~5음 시그니처로 기억성을 만든다.
- 모든 효과음은 마스터 볼륨과 SFX 볼륨을 분리하고, 앱 복귀 시 중복 재생하지 않는다.
- 게임별 고유 사운드는 공통 이벤트를 해치지 않는 범위에서 1~2개만 사용한다.
- 햅틱은 `tap / good / miss / combo / win` 강도를 구분하고, 사용자가 끌 수 있어야 한다.
- Android 오디오 포커스 손실, 무음 모드, 백그라운드 전환, 이어폰 연결/해제를 테스트한다.

### 1차 사운드 제작 목록

- Mallow 짧은 목소리 없는 반응음 5종
- UI 탭/선택/닫기 3종
- 정답/오답/콤보/최고 기록 4종
- 게임 시작/카운트다운/시간 경고 3종
- 결과 완료/미션 완료 2종
- 홈용 저자극 루프 1종, 게임용 루프 2종

총 20개 안팎으로 시작하고, 플레이테스트에서 피로도를 확인한 후 늘린다.

## 7. 권장 Godot 구조

```text
mobile/
└─ godot/
   ├─ project.godot
   ├─ app/
   │  ├─ AppShell.tscn
   │  ├─ Home.tscn
   │  ├─ Games.tscn
   │  ├─ Records.tscn
   │  ├─ Result.tscn
   │  └─ Settings.tscn
   ├─ autoload/
   │  ├─ app_state.gd
   │  ├─ save_store.gd
   │  ├─ audio_director.gd
   │  ├─ haptics.gd
   │  ├─ i18n.gd
   │  └─ analytics.gd
   ├─ games/
   │  ├─ flash/
   │  ├─ bubble/
   │  └─ trace/
   ├─ data/
   │  ├─ games.json
   │  ├─ missions.json
   │  └─ locales/
   ├─ assets/
   │  ├─ art/
   │  ├─ audio/
   │  └─ fonts/
   └─ tests/
```

### 데이터 계약

웹과 Android가 장기적으로 공존하려면 게임 구현이 아니라 데이터 계약을 먼저 고정한다.

- `GameDefinition`: id, 인지 영역, 난이도, 지원 언어, 설명, 대표 색, 썸네일
- `GameResult`: game id, score, level, duration, mistakes, createdAt, appVersion
- `PlayerProfile`: locale, sound, music, haptics, streak, lastSeen
- `DailyMission`: date(KST), game ids, completed ids
- `SaveSchema`: version, records, profile, mission, importedFrom

저장 위치는 Godot `user://` 아래 버전이 붙은 JSON으로 시작한다. 추후 계정이 생겨도 서버 모델을 이 계약에 맞추면 된다. 기존 웹 백업 코드 `MLW1.`은 가져오기 화면에서만 읽고, 가져온 데이터는 새 스키마로 변환한다.

## 8. 단계별 일정과 종료 조건

### Phase 0 — 툴체인·디자인 기준선

- Godot 4.7.1 stable, Android Studio, JDK 17, Android SDK/NDK 설치
- package id, 앱 서명 키, 버전 정책 확정
- Mallow UI 토큰과 사운드 이벤트 표 확정
- 세로 화면 360×800 기준 첫 화면 목업 확정

종료 조건: 빈 Godot 앱이 실제 Android 기기에서 설치·실행·백그라운드 복귀된다.

### Phase 1 — Vertical slice

- `flash`, `bubble`, `trace`
- 홈/결과/기록/설정
- 로컬 저장, MLW1 가져오기, 사운드·햅틱
- 한국어 우선, 영어 문자열 구조까지 포함

종료 조건: 중급 Android 기기에서 20분 반복 플레이 동안 치명적 오류 0, 터치 오작동률 허용 범위 내, 결과·기록 재실행 검증 완료.

### Phase 2 — 콘텐츠 묶음 포팅

- 기억: `count`, `cards`, `rev`
- 속도/관찰: `react`, `spot`, `odd`
- 논리/공간: `nono`, `fit`, `sort`
- 청각: `melody`, `rhythm`, `pitch`

종료 조건: 공통 셸 수정 없이 게임 모듈을 추가할 수 있고, 일일 미션·추천·기록에 자동 편입된다.

### Phase 3 — 서비스화

- Android Share Sheet, 딥링크
- 분석 이벤트 정리와 오류 수집
- Play 내부 테스트 → 비공개 테스트 → 공개 출시
- 필요 시 로그인/클라우드 백업/광고/결제

## 9. 출시 전 QA 게이트

| 영역 | 반드시 확인할 시나리오 |
|---|---|
| 기기 | 저가·중급·고급 Android, 작은 폰·대형 폰·태블릿 |
| 입력 | 빠른 연속 탭, 드래그 중 손가락 이탈, 멀티터치, 화면 가장자리 |
| 생명주기 | 홈 이동, 전화/알림, 화면 잠금, 앱 강제 종료 후 복귀 |
| 오디오 | 무음 모드, 블루투스, 이어폰, 오디오 포커스, 설정 토글 |
| 저장 | 첫 실행, 기록 갱신, 날짜 변경, 저장 손상, MLW1 가져오기 |
| 시간/언어 | KST 자정, 영어/태국어 폰트·긴 문자열·RTL 오해 여부 |
| 성능 | 60fps, 메모리, 배터리, 첫 실행·게임 전환, 오프라인 |
| 배포 | 서명된 AAB, target API 정책, 개인정보처리방침, 앱 아이콘/스크린샷 |

Google Play는 2026-08-31부터 신규 앱과 업데이트에 Android 16(API 36) 이상 target을 요구하므로, 실제 프로젝트를 시작할 때 target API 36 지원 여부를 엔진·플러그인 조합과 함께 확인해야 한다.

## 10. 다음 실행 순서

1. 개발 머신에 Godot 4.7.1 stable과 Android toolchain을 설치한다.
2. `mobile/godot`를 별도 프로젝트로 만들고 빈 앱을 기기에 배포한다.
3. 이 문서의 데이터 계약과 디자인 토큰을 먼저 코드화한다.
4. `flash → bubble → trace` 순서로 vertical slice를 구현한다.
5. 실제 기기 QA 후 Phase 2 포팅 여부를 결정한다.

현재 저장소에는 네이티브 엔진 프로젝트가 없고 Godot/Android 실행 도구도 감지되지 않았으므로, 이번 검토에서는 웹 앱 코드를 임의로 대규모 변경하지 않는다. 다음 개발 턴부터는 `mobile/godot`를 별도 영역으로 추가해 웹 서비스와 충돌 없이 진행한다.

## 참고한 공식 문서

- [Godot 4.7.1 stable archive](https://godotengine.org/download/archive/)
- [Godot: Exporting for Android](https://docs.godotengine.org/en/stable/tutorials/export/exporting_for_android.html)
- [Godot: User interface](https://docs.godotengine.org/en/stable/tutorials/ui/index.html)
- [Godot: Integrating with Android APIs](https://docs.godotengine.org/en/stable/tutorials/platform/android/javaclasswrapper_and_androidruntimeplugin.html)
- [LÖVE: Game Distribution](https://love2d.org/wiki/Game_Distribution)
- [LÖVE Android port](https://github.com/love2d/love-android)
- [Google Play target API level requirements](https://support.google.com/googleplay/android-developer/answer/11926878?hl=en)
