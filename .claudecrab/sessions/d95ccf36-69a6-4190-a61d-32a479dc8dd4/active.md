# Active Session State

## 진행 중인 작업
없음 — **0단계(기록 지표 정직화) + 0.5단계(판별 계측)** 2026-08-03 완료.
상세=CLAUDE.md 첫 항목, 패치=`.logs/patch_curriculum0.mjs`, 검증=`.logs/curr0_check.mjs`.

## 다음 단계(사용자 승인 완료, 데이터 대기 중)
사용자의 "훈련 코스" 제안을 재배열한 4단계 계획 중 0·0.5단계만 실행함. 남은 것:
- **1단계 — 2026-08-20 이후**: `game_result` 이벤트가 2~3주 쌓이면 확인할 것
  = ㉠봇이 실제로 재도전률을 올렸는가(`bot:'lose'` 다음 판이 이어지는 비율)
    ㉡난이도 상승이 일어나는가(`diff` 분포 변화) ㉢`ratio` 분포가 70~85%대에 있는가.
  **봇이 효과 있을 때만** 봇을 '점수 목표'에서 '게임 자체 난이도'로 확장한다.
- **2단계**: 코스 2개 시범(효능 중립 이름, 예 "이어하기 코스"). **전이 테스트는 만들지 않는다**
  — 근거는 CLAUDE.md 첫 항목(FTC v. Lumos Labs 2016-01, $2M) + MEMORY.md 효능 주장 금지선.

**재사용할 교훈 ①**: 도박·복권 성격의 요청은 만들지 않는다(근거=MEMORY.md "콘텐츠 금지선").
거절로 끝내지 말고 **같은 의도를 도박 없이 달성하는 대안**을 만들어 낼 것.

**재사용할 교훈 ②**: 공유 미리보기 크롤러(카톡·페북·라인·트위터)는 **JS를 실행하지 않는다**.
og태그를 JS로 갈아끼우는 방식은 무효. 언어/상태별 미리보기가 필요하면
`.logs/make_share_pages.mjs` 패턴(얇은 전용 페이지 + `vercel.json` rewrite)을 쓸 것.
경로는 반드시 **슬래시 없이**(`/en`, `/queens/en`) — 슬래시를 붙이면 기준 디렉터리가 바뀌어
상대경로 자산이 전부 404. 검증 스크립트: `.logs/share_lang_check.mjs`,
`lang_land_check.mjs`, `sub_lang_check.mjs` (인자로 실서버 주소를 주면 그대로 검사).

**재사용할 교훈 ③**: 34종 전부에 뭔가를 걸어야 하면 **`afterGame(id,r)` 한 곳**이면 된다
(`missionMark`=아케이드 31종, `saveFlatBest`=평점 5종이 전부 여기로 모인다).
난이도 없는 게임은 `diff:'all'`로 들어온다.

## 배포 방법(확인됨)
로컬 브랜치는 `master`, 원격 배포 브랜치는 `main`. 푸시는
`git push "https://x-access-token:$GITHUB_TOKEN@github.com/budlove-billy/typing.git" master:main`
(토큰은 `.env`의 `GITHUB_TOKEN`). 푸시 후 1분이면 Vercel 반영.

## 미결(사용자 결정 대기)
- **애드센스 — 사용자 차례(심사 요청).** 퍼블리셔 ID `ca-pub-8615421634491307` 연결 완료
  (27쪽 head 스니펫 + 루트 `ads.txt`). 사용자가 대시보드에서 **"검토 요청"**을 눌러야 심사 시작.
  승인되면 내 할 일 = `애드센스-신청-가이드.md`의 안전 위치에만 광고 단위 배치,
  **게임 플레이 화면·버튼 근처·팝업 위는 영구 금지**.
- 레거시 `brain_app.html` 정리 여부(미배포·미링크)
- GA4 리뷰 리마인더 미실행(사용자 확인 없이는 실행 금지)
- 운세 페이지들에 ko/en/th 고정 SEO 블록 + hreflang 소급 적용 여부(별건, 승인 필요)

## 참고
- nono는 점수 눈금이 새로 잡혔지만 옛 기록 환산에서는 제외됨(정직한 환산 기준이 없어서).
  사용자가 "노노그램 기록이 이상하다"고 하면 이 사실을 먼저 설명할 것.
- 기존 사용자는 이번 개편으로 표시 숫자가 내려간다(다양성 30% 제거). 이름을
  '종합 두뇌 레벨'→'훈련 숙련도'로 바꾼 것이 그 완충이다. 문의가 오면 이렇게 설명할 것.
