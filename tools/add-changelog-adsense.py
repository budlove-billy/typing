# -*- coding: utf-8 -*-
"""CHANGELOG 맨 위에 항목 추가. 이 파일은 CRLF라 개행을 그대로 맞춰야 한다
   (LF로 '# Changelog\\n'을 찾으면 매칭이 안 돼 조용히 아무 일도 일어나지 않는다)."""
import io

ENTRY = """- 2026-08-23 — **애드센스 '가치가 별로 없는 콘텐츠' 반려 대응 — 57 URL 전부 기준선 위로**: 원인은 광고를 다는 페이지 대부분이 `/?game=x`로 보내는 안내문이었다는 것 — 구글이 도어웨이 페이지라 부르는 형태다. 57개 URL을 실제로 렌더링해 재니(`.logs/thin_audit.mjs`) **1,200자 미만이 18개**였고, 최악은 `/tarot/?lang=th`의 **221자**였다. 타로에 태국어 섹션이 없어서 th 모드로 들어오면 한국어 본문이 통째로 숨고 빈 페이지가 되는데, 그 위에 자동광고가 붙고 있었다.
  **HTML로는 광고를 뗄 수 없다는 점이 방향을 정했다.** 소스에 수동 광고 유닛(`<ins>`)이 하나도 없고 렌더에 잡히는 것은 전부 구글 자동 광고라, 배치 제어는 대시보드에만 있다. 즉 '얇은 페이지에서 광고만 빼기'가 불가능하므로 **콘텐츠를 채우는 것이 유일한 레버**였다.
  글자수 패딩은 같은 판정을 다시 받으므로, 각 페이지에서만 얻을 수 있는 내용을 썼다 — 스도쿠는 숨은 단수·단독 후보·짝 후보·줄–박스 상호작용과 난이도별로 어디까지 필요한지, 네모로직은 겹치기 계산 `(숫자×2)−줄 길이`와 여유 적은 줄부터 푸는 순서, 2048은 코너 고정 이후 무너지기 직전의 신호, 반응속도는 브라우저 측정에 화면 주사율·입력 지연이 섞인다는 것과 Go/No-Go의 맞바꿈. 한국어 랜딩 12곳 + 홈 + 타로 + 가이드 2곳 + **en/th 랜딩 16곳**(8/18에 생성기로 한 번에 찍어 낸 파일들이라 가장 얇게 남아 있었다. 직역이 아니라 각 언어로 새로 씀).
  **허위 문구도 두 곳 있었다** — `sudoku`가 광고를 달고서 "광고도 없습니다", `water-sort`가 "광고 방해 없이"라고 적혀 있었다. 7/31에 이 카피를 정리하며 `2048` 한 곳만 잡고 "grep 결과 한 곳뿐"이라고 기록했는데 부정확했다. "광고 **방해** 없이"처럼 사이에 낱말이 끼면 단순 grep이 놓친다.
  **사실 확인한 것**: 스킬 게임은 HOME_GAMES 기준 36개이고 36개 전부 `screen-<id>`가 존재한다 — 홈 카피가 ko·en 33 / th 31로 서로 달랐고 `guide/brain-games`는 33종만 나열해 제목과는 맞지만 홈과 어긋났으므로, 가이드에 빠진 말로우 런·영단어·한글 타자를 채우고 전부 36종으로 통일했다. 슬라이딩 퍼즐 가이드에 "못 푸는 판이 없다"를 쓰기 전에 `slShuffleTiles()`가 완성 상태에서 가능한 이동만 반복함을 코드로 확인했다. 수능 교시별 시각은 검색으로 확정되지 않아 **싣지 않고** 평가원 공고 확인을 안내했다(시행일 2026-11-19은 셋째 목요일 관례와 요일 계산이 모두 맞아 그대로 뒀다).
  **측정 오류를 하나 냈다가 잡았다.** 처음에 `?lang=th` 6쌍이 한국어판과 자카드 1.0으로 나와 중복 콘텐츠로 보고할 뻔했는데, Playwright 컨텍스트를 URL 간 공유해 `localStorage.brain.lang`이 새어 뒤 URL이 앞 언어로 렌더된 탓이었다. URL마다 `newContext()`로 격리하니 **중복 0쌍**. 언어 변형을 잴 때는 컨텍스트를 반드시 분리할 것.
  결과: **1,200자 미만 18 → 0**, 최소 페이지 221자 → 1,246자, 중복 0쌍. 검증은 `thin_audit.mjs`(57 URL) · `en_landing_check.mjs`(32조합, 한글누수·CTA·JSON-LD·320px 넘침) · `hreflang_check.mjs` · `en_seo_check.mjs` 전부 ALL PASS. 사이트맵 lastmod도 갱신(39개가 오늘 날짜). `tools/android/`의 SDK HTML 수백 개는 gitignore되어 배포되지 않으므로 무관했다.
  **남은 것**: 사용자가 애드센스 대시보드에서 검토 요청 + Search Console 사이트맵 재제출. 태국어는 여전히 원어민 검수 미실시(용어 일관성만 확보).

"""

f = 'docs/CHANGELOG.md'
s = io.open(f, encoding='utf-8', newline='').read()
nl = '\r\n' if '\r\n' in s[:200] else '\n'
head = '# Changelog' + nl + nl
assert s.startswith(head), '머리말 형식이 예상과 다름'
assert '애드센스' not in s[:3000], '이미 적용됨'
body = ENTRY.replace('\n', nl)
s = head + body + s[len(head):]
io.open(f, 'w', encoding='utf-8', newline='').write(s)
print('ok CHANGELOG (%s)' % ('CRLF' if nl == '\r\n' else 'LF'))
